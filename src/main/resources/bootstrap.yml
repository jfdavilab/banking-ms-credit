spring:
  application:
    name: ms-credit
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 3
        initial-interval: 1000